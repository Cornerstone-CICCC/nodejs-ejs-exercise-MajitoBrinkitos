<%- include('partials/header', { pageTitle: 'Home' }) %>

<main>
    <h1><%= title %></h1>
    <p><%= welcomeMessage %></p>

<!--     <% if (showYear) { %>
        <p>Today's Year: <%= new Date().getFullYear() %></p>
    <% } %> -->

    <img src="/images/home-page.png" alt="fake image"/>
</main>

<%- include('partials/footer') %>   